server:
  port: 8080
  tomcat:
    max-threads: 500
    min-spare-threads: 50

spring:
  application:
    name: ${rootArtifactId}-admin-java
  jackson:
    default-property-inclusion: non_null
  profiles:
    active: live

# apollo配置
#apollo:
#  bootstrap:
#    enabled: true
#    namespaces: application,application.yml
